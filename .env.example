# Neon Postgres connection string (https://neon.tech)
DATABASE_URL="postgres://user:password@host/db?sslmode=require"

# No ANTHROPIC_API_KEY here — this app uses BYOK (each visitor supplies their own
# key in the UI; it's stored in localStorage and sent per-request).
